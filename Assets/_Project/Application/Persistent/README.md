# Persistent

Application-lifetime composition.

Examples:

```text
persistent content scene
physical Camera output
persistent presentation root
application-level UI/EventSystem when appropriate
provisioning infrastructure when application-owned
```

Persistent objects are not automatically runtime authority.
