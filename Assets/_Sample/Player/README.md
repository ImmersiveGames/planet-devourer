# Player

Frozen initial Demonstration Applications:

```text
Scene-Provided
Manager-Provisioned
Character Selection
Local Multiplayer
```

The group README compares the applications.

There is no global Player HUB that silently switches the Active GameApplication.
The consumer explicitly selects the application/GameApplication and uses the official `Set Active` workflow.

Player capabilities are Scenarios by default, not separate applications.
